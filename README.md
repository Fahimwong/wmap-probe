# World Mini App allowlist probe

Throwaway research page for a one-off experiment: does a World App Mini App
permit transactions that touch **dynamically-created** contracts and tokens?

Contains a single static HTML file. No secrets, no keys, no backend.
All addresses referenced are public World Chain mainnet contracts.

Delete this repository once the experiment is finished.
